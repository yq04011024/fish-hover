# 更新日志

本插件所有 notable 变更记录在此文件中。

## [0.3.0] - 2026-09-18

### 新增
- **独立「抖音视界」**（活动栏新入口，与 B 站视图互不影响）：
  - 竖向沉浸式信息流：scroll-snap 逐屏吸附，滚轮 / ↑↓ 键 / 命令切换，当前视频自动播放、其余暂停
  - 分区：推荐 / 关注（关注流接口要求 a_bogus 签名，内置自研纯算法签名 `dy-signer.js`，与参考实现逐字节一致）
  - 本地媒体代理：`127.0.0.1` 随机端口 HTTP 代理转发视频流，附带 Cookie/UA/Referer、手动跟随重定向、透传 Range（支持进度条拖动），解决 webview 直连抖音 CDN 403
  - 抖音 Cookie 配置：标题栏 Cookie 按钮 / `biliHover.douyinSetCookie` 命令 / `biliHover.douyinCookie` 配置项；未配置时页面引导；配置变更自动重载
  - 内存保护：仅保留当前 ±1 视频的 src，其余回收；列表上限 60 条
  - 播放失败自动换下一个直链（最多 3 次）；仅 HLS 流的视频提供「在浏览器打开」兜底
- 新增配置：`biliHover.douyinCookie` / `biliHover.douyinAutoPlay` / `biliHover.douyinVolume`
- 新增命令：`biliHover.douyinRefresh`（刷新）/ `biliHover.douyinSetCookie`（设置抖音Cookie）/ `biliHover.douyinPrevVideo` / `biliHover.douyinNextVideo`

### 修复
- 配置 Cookie 后无法加载（对照 TouchFish 可用实现排查）：请求层由 undici `fetch` 改为 node http/https 模块（TLS/头指纹差异被抖音风控拦截）；推荐流补齐 a_bogus 签名与 `Referer: /?recommend=1`、`platform=PC`、`timestamp` 参数；关注流对齐精简参数集与 Chrome/129 UA（签名与请求头一致）、`Referer: /follow`；修正关注流响应解析（`data[].aweme` + 顶层 `cursor/has_more`，此前必然解析为空列表）；Cookie 值清洗换行等非法头字符

### 说明
- 抖音推荐流与关注流均需登录态 Cookie（实测无 Cookie 返回空数据），不提供游客流
- 一期不包含：直播、点赞/评论交互、HLS 内播（回退浏览器打开）、下载

## [0.2.3] - 2026-09-18

### 新增
- 稍后再看：悬停视频卡片出现时钟按钮，一键加入 B 站「稍后再看」列表（POST toview/add + CSRF，需登录）
- 不伪装模式新增「登录/退出登录」用户区与 B 站扫码登录弹窗，登录成功自动写入 Cookie

### 修复
- 直播流解析 404（旧接口 `room/v1/Room/playUrl` 已下线）：改用 `xlive/web-room/v2/index/getRoomPlayInfo`，按 http_hls(fmp4) → http_hls → http_stream 优先级取流（同 TouchFish）
- 播放方式重构：取消底部悬浮播放条，改为卡片封面原位播放（含直播），右上角 ✕ 关闭恢复封面

### 调整
- 移除「动态」分区
- 版本号与文档同步

## [0.2.2] - 2026-09-18

### 优化
- 不伪装模式卡片样式对齐B站网页风格：封面左下叠加播放量/弹幕数（图标+数字）、封面圆角、标题两行加粗、悬停标题变B站粉
- 作者行改为「UP主头像 + 昵称 + 发布日期」布局（与B站网页一致），数据源补充头像与发布日期字段
- 播放中的卡片高亮改为B站粉色描边

## [0.2.1] - 2026-09-17

### 新增
- 按伪装开关读取对应文件渲染，切换设置立即重新渲染
- 标题栏新增「切换是否伪装」按钮（eye-closed 图标），一键切换双形态并立即生效
- 不伪装模式随机播放改为在侧边栏内原位播放（列表未就绪时自动等待补播）

### 修复
- 随机播放按钮图标无效（原 `$(shuffle)` 非 VS Code 内置图标，改为 `$(play-circle)`）
- 不伪装模式下观看样式改为视频在卡片封面位置原位播放（替代底部悬浮条方案）

### 调整
- 描述文案与「是否伪装」默认值（false）保持一致

## [0.2.0] - 2026-09-16

### 新增
- 「是否伪装」核心开关（默认关闭）：
  - 关闭：侧边栏为 webview 视频流卡片列表，侧边栏内直接播放
  - 开启：侧边栏为视频列表，点击在编辑区打开播放面板——面板伪装为本地代码文件，画面被遮罩、聚焦显示/失焦隐藏
- 伪装面板遮罩可渲染本地文本文件内容（编辑器风格带行号，最多 600 行）
- 两种形态的侧边栏视图通过 when 子句互斥自动切换
- 搜索结果分页（上一页/下一页命令）
- 侧边栏标题可自定义（重启生效）

### 优化
- 搜索改为原生 InputBox 方式，去掉内嵌搜索框
- 播放流请求失败时给出 Cookie 失效引导

## [0.1.0] - 2026-09-15

### 新增
- 首个可用版本：侧边栏推荐视频列表（随机推荐 / Cookie 个性化推荐）
- 编辑区播放面板：悬停显示画面、移开恢复纯黑遮罩，音频持续
- 关键词搜索B站视频
- Cookie 手动粘贴配置（设置项 + 标题栏按钮）
- 面板宽度拖拽记忆、自动播放与初始音量配置
